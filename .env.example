# Copiar a .env.local y completar con valores reales. No versionar .env.local.

# URL del backend a consumir desde el navegador (via proxy Nginx). En
# desarrollo es localhost:8080; en producción, el dominio real de la API.
NEXT_PUBLIC_BACKEND_URL="http://localhost:8080"
